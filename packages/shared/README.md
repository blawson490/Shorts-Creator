# Shared Package

Shared job contracts for both web and worker apps.

## Contents
- `src/jobs.ts`: typed job payload and contract definitions.
- `schemas/jobs.schema.json`: JSON Schema covering `ingest`, `transcribe`, `score`, and `render` jobs.
